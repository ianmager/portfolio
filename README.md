# Portfolio

Personal portfolio site built with [Astro](https://astro.build) and deployed on GitHub Pages.

## Quick start

```bash
npm install
npm run dev        # local dev server at localhost:4321
npm run build      # production build to dist/
npm run preview    # preview production build locally
```

## Deploying

This repo ships with a GitHub Actions workflow (`.github/workflows/deploy.yml`) that automatically builds and deploys to GitHub Pages on every push to `main`.

## Link
Github pages link (https://ianmager.github.io/portfolio/)